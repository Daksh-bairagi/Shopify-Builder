services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: shopmirror
      POSTGRES_USER: shopmirror
      POSTGRES_PASSWORD: shopmirror
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
